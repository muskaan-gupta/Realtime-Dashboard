# Vercel Production Environment Variables
# Add these in your Vercel project dashboard: Settings → Environment Variables

# MongoDB Configuration (Use your production MongoDB credentials)
MONGODB_URI=mongodb+srv://prod_username:prod_password@cluster.mongodb.net/realtime-analytics

# JWT Configuration (Generate secure production secrets)
JWT_SECRET=generate-a-secure-64-character-random-string-for-production
JWT_EXPIRES_IN=7d

# App Configuration (Replace YOUR_PROJECT_NAME with your actual Vercel project name)
NEXTAUTH_SECRET=generate-another-secure-64-character-random-string-for-production
NEXTAUTH_URL=https://YOUR_PROJECT_NAME.vercel.app

# Server Configuration
PORT=3000
NODE_ENV=production

# IMPORTANT INSTRUCTIONS:
# 1. DO NOT use the same credentials as development
# 2. Generate new secure secrets using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# 3. Create separate MongoDB user for production with limited permissions
# 4. Add these variables in Vercel dashboard, NOT in your code
# 5. Replace YOUR_PROJECT_NAME with your actual Vercel project name